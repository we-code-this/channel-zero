import DigHeader from "../../components/common/DigHeader";
import Layout from "../../components/common/layouts/Layout";

const Release = () => {
  const header = <DigHeader />;

  return (
    <Layout header={header}>
      <h1>Dig release page</h1>
    </Layout>
  );
};

export default Release;
